<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hh.his.graduationproject.dao.MenuRoleMapper" >
    <resultMap id="BaseResultMap" type="MenuRole" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="menu_id" property="menuId" jdbcType="INTEGER" />
        <result column="role_id" property="roleId" jdbcType="INTEGER" />
    </resultMap>
    <sql id="Base_Column_List" >
    id, menu_id, role_id
  </sql>

    <delete id="deleteByRid">
        delete from menu_role where role_id=#{rid}
    </delete>

    <delete id="deleteByMid">
        delete from menu_role where menu_id=#{mid}
    </delete>

    <insert id="insertRecord">
        insert into menu_role (menu_id,role_id) values
        <foreach collection="mids" separator="," item="mid">
            (#{mid},#{rid})
        </foreach>
    </insert>


    <insert id="insert">
        insert into menu_role (menu_id,role_id) value
            (#{mid},#{rid})

    </insert>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
        delete from menu_role
        where id = #{id,jdbcType=INTEGER}
    </delete>


</mapper>